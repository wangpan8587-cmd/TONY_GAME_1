# 禁止删除，必读：
【代码修改约束规则】
1. 禁止重复犯同类编码错误（如语法错误、逻辑漏洞、边界值处理缺失等），修改前先检查历史同类问题并规避；
2. 代码修改遵循“最小改动原则”：仅修改目标功能对应的代码块，不触碰无关变量、函数、注释、格式；
3. 绝对避免“牵一发而动全身”：修改前明确代码影响范围，不修改未提及的依赖、配置、接口，不擅自新增/删除非目标逻辑；
4. 改动后必须验证：确保仅目标问题修复，原有正常功能不受任何影响，无新增BUG。

# 开发日志与心得

## [2026-02-14] 流程优化与主题展示

### 待办任务
1. [x] 整体流程重构（Start -> Tutorial -> Identity -> Intro -> Playing）
2. [x] 打字机播报内容切换为 `STORY_INTRO`
3. [x] 在 `DayTransition` 界面展示 `dayThemes`
4. [x] 优化卡牌拖拽预告：移除数值，改为纯视觉暗示

### 修改记录与心得
- **变更内容**：
  - 移除了 `StatBar` 预告中的明确数值（不再展示 `↑ 10` / `↓ 5`）。
  - 改为更含蓄的“方向 + 强弱”提示：
    - 方向：`▲` / `▼`
    - 强弱：小变化显示单个符号，大变化显示双符号（`▲▲` / `▼▼`）
    - 强度随拖拽距离渐变（透明度呼吸 + 宽度变化），保持“弱预告”。
  - 进度条预告层从中性灰投影调整为**同方向色系的轻微光晕**（绿/红、低透明度）。

- **潜在错误与规避**：
  - 避免“信息过直白”：彻底不输出数值，防止玩家用数学方式最优解。
  - 避免“过于显眼”：所有预告元素透明度受 `progress` 控制且上限较低。

- **设计心得**：
  - “方向+强弱”的符号提示在不暴露数值的前提下，仍能传达决策风险。
  - 光晕比整段投影更符合“弱提示”，并且更容易让玩家感知上升/下降趋势。

---

## [2026-02-14] 结局/失败页增加结束原因反馈
- **变更内容**：
  - 在 `FailureScreen`（失败页）增加了“结束原因：客观原因（主观标签）”的展示，如“面子归零（在乡亲面前彻底失去了体面）”。
  - 在 `EndingScreen`（通关页）增加了“达成原因：客观描述（心理特质）”的展示，如“游刃有余（面子与里子保持了微妙的平衡）”。
  - 视觉上采用半透明背景的 Pill 标签形式，失败页用淡红色系，通关页用淡琥珀色系。
- **潜在风险**：
  - 判定逻辑需与 `gameReducer` 中的结局/失败判定条件严格一致。
  - 文本长度增加，在窄屏手机上需注意不要挤压底部按钮。
- **设计心得**：
  - 组合式（客观+主观）的原因描述，既能解答玩家对数值的疑惑，又能通过文字提升代入感。

---

## [2026-02-14] 维护规则更新
- **新增原则**：`README.md` 中不允许删除任何过去的内容，只允许新增（Append Only）。所有历史记录、错误分析及心得必须完整保留，以维护开发脉络。

---

## [2026-02-14] 新增功能：卡牌进程指示器

### 设计方案
- **需求**：在卡牌上方增加进程展示，每天 3 张牌。
- **方案**：
  - 在 `GameBoard` 顶部增加 `CardProgressIndicator` 组件。
  - 使用 3 个水平分布的分段小块表示当前进度。
  - 状态分：完成（深灰）、当前（高亮红）、未到（浅灰）。
- **潜在风险**：
  - 布局挤压：卡牌在不同手机屏幕高度下可能被遮挡 or 压缩，需要确保 `flex` 布局的灵活性。
  - 状态同步：确保 `round` 更新时，指示器的动画能同步响应。

---

## [2026-02-14] 每日总结页新增“今日变化”（A2：符号 + 强弱短条）
- **需求**：在 `DaySummary` 页面体现当天（3 张卡累计）的面子/里子变动趋势。
- **口径**：
  - `delta = stats - dayStartStats`
  - 阈值分档：
    - `|delta| <= 5`：`—`（几乎无变化）
    - `6~14`：`▲/▼`（小幅）
    - `>= 15`：`▲▲/▼▼`（明显）
- **表现**：
  - 每项一行：左侧 label（面子/里子），右侧趋势符号 + 一条淡色短条（小幅 50%，明显 100%）。
  - 上升：淡绿；下降：淡红；无变化：淡灰。
- **心得/注意事项**：
  - 不展示具体数值，避免“算账式”玩法。
  - 使用 `dayStartStats` 能确保是“每日累计结果”，不会被单张卡牌噪音干扰。

---

## [2026-02-14] 结局/失败页增加结束原因

### 设计方案
- **失败页**：根据导致失败的数值判定原因。
  - 面子归零：【面子尽失】
  - 里子归零：【身心俱疲】
  - 面子爆满：【虚伪透顶】
  - 里子爆满：【格格不入】
- **通关页**：根据最终数值区间评价特质。
  - 平衡：【游刃有余】
  - 偏向：【极度自我】/【极度体面】
- **表现形式**：在标题上方/下方增加一行强调文案。

## [2026-02-14] 结局/失败页文本排版优化（方案 B）
- **变更内容**：
  - 将结局/失败正文按自然段分段渲染：优先按 `\n\n` 分段，段内按 `\n` 拆行。
  - 增加行高（`leading-relaxed`）与段间距（`mb-4`），缓解“文字墙”。
  - 对话句（包含「」或“ ”）轻微突出：更高对比度颜色 + `italic` + 左侧淡边框（`border-l-2 ... pl-3`）。
  - 限制正文最大宽度并居中（`max-w-sm mx-auto`），增强移动端可读性。
  - 文本较长时允许正文区域滚动（`overflow-y-auto`），避免挤压底部按钮。
- **心得**：
  - 排版就是信息架构；对话与叙述区分后，玩家能更快抓住情绪锚点。

## [2026-02-14] 卡牌文案展示优化（description/consequence）
- **变更内容**：
  - 抽象通用 `renderRichText(text, mode)`，统一“分段渲染 + 行高/段距 + 对话句轻微突出”的规则。
  - 将 `SwipeCard` 正面 `card.description` 改为使用 `renderRichText(..., 'cardFront')` 渲染。
  - 将 `SwipeCard` 背面 `lastChoice.consequence` 改为使用 `renderRichText(..., 'cardBack')` 渲染，并对长文本开启内部滚动，避免遮挡“点击继续”。
  - 同步将 `FailureScreen` / `EndingScreen` 的正文改为使用通用渲染器（`failure` / `ending` 模式），减少重复逻辑。
- **潜在风险**：
  - 个别卡牌若包含极长单行文本，仍可能出现滚动需求；已通过 `overflow-y-auto` 处理。
- **心得**：
  - 将富文本渲染逻辑收敛为一个函数后，后续优化（如对话识别规则、段落间距）可一次性全站生效。

## [2026-02-14] 新原则：整屏可读（禁止滚动条）与分页展示
- **新增原则**：任何界面文案展示不得依赖上下滚动条；必须通过“分页/逐段推进/摘要”保证整屏可读。
- **默认行数预算（在原值基础上 +4）**：
  - 卡牌正面摘要：9 行
  - 卡牌正面分页：12 行
  - 卡牌背面分页：12 行
  - 结局页分页：14 行
  - 失败页分页：14 行
- **交互调整**：卡牌正面移除“摘要/展开”二分逻辑，默认直接以分页模式呈现最佳展示效果；点击正文仅用于翻页（最后一页回到第一页）。

---

## [2026-02-14] 维护规则更新（补充）
- **新增原则**：`README.md` 允许自主新增内容；每条功能/修复更新完成之后，必须将更新记录以“追加”的方式写入本文件（Append Only），并将该条作为长期维护规则执行。

---

## [2026-02-14] 身份解锁条件完善 + 通关激励提示

- **变更内容**：
  - **身份解锁条件调整（基于成功通关）**：
    - 外卖骑手（`waimai`）：大厂卷王（`juanwang`）或体制内青年（`tizhinei`）任意一个达成 7 天游玩并进入任意成功结局后解锁。
    - 家族继承人（`fuerdai`）：大厂卷王（`juanwang`）+ 体制内青年（`tizhinei`）+ 外卖骑手（`waimai`）三者都成功通关后解锁。
  - **存档字段新增**：新增 `successfulIdentities` 用于记录“成功通关过的身份”。
  - **旧存档兼容/修复**：加载存档时会将 `completedIdentities` 同步补齐到 `successfulIdentities`，防止玩家“已通关但无法解锁”的问题。
  - **修复 bug**：`IdentitySelect` 漏传 `savedData` 导致解锁状态无法正确判定，已修复为传入 `savedData={state.savedData}`。
  - **通关激励提示**：在 `EndingScreen` 结局页增加解锁/进度提示（基于成功通关、任意成功结局）：
    - 本局若刚解锁外卖骑手：提示“恭喜通关！您已成功解锁【外卖骑手】身份，快去体验吧！”
    - 本局若刚解锁家族继承人：提示“恭喜通关！您已成功解锁【家族继承人】身份，快去体验吧！”
    - 若已成功通关所有身份：提示“恭喜你通关所有身份，但还有更多的结局等你挑战！”
    - 否则提示“还差哪个身份成功通关即可解锁家族继承人”，引导继续游玩。

- **设计目的**：
  - 让玩家在通关后立即获得“奖励感”（新身份解锁）。
  - 在未解锁富二代时明确下一步目标，降低迷茫感，提升继续游玩意愿。
  - 全身份通关后将动机切换到“结局收集”。

---

## [2026-02-15] 结算页 UI 强化与引导优化

- **变更内容**：
  - **结果横幅（Result Banner）**：在 `FailureScreen` 和 `EndingScreen` 顶部增加了显眼的结果横幅。
    - 失败页：展示红色渐变横幅“挑战失败”，并伴随轻微下坠动画。
    - 成功页：展示金色渐变横幅“通关成功”，并伴随轻微浮现动画。
  - **失败引导增强**：在失败页增加了“继续挑战”进度面板，展示身份、成就、结局的解锁进度。
  - **玩法建议**：根据失败原因动态给出玩法建议（如：面子爆满建议适当示弱）。
  - **快速重试**：失败页新增“换个身份再战”按钮，减少挫败感并鼓励探索。

- **设计目的**：
  - 强化成功与失败的视觉对比，让玩家一眼识别结果。
  - 通过进度展示和明确建议，将失败转化为“再来一把”的动力。

---

## [2026-02-15] 结算页单屏布局优化（禁止滚动/翻页）

- **变更内容**：
  - 将 `FailureScreen` / `EndingScreen` 调整为 `h-screen + overflow-hidden` 的三段式布局（顶部结果横幅 / 中部内容 / 底部进度+按钮），确保按钮不被顶出屏幕。
  - **正文展示改为摘要模式**：不滚动、不翻页，固定显示 8 行，超出部分截断（`-webkit-line-clamp: 8`）。
  - **字号层级优化**：结果横幅/标题保持醒目，正文改为 `text-[13px] leading-5`，在小屏下优先保证按钮可见与整体清晰。
  - **底部区域压缩**：进度展示改为横向紧凑排版，按钮改为双列布局（换个身份/重新开始）或单主按钮。

- **设计目的**：
  - 解决结算页内容拥挤、按钮超出屏幕的问题。
  - 在不允许滚动的约束下，用“摘要化”保留叙事氛围并确保 CTA 可达。

---

## [2026-02-15] 卡牌选项（choices）视觉强化

- **变更内容**：
  - **字号与对比度提升**：卡牌底部选项字号从 `text-xs` 提升至 `15px`，并将字体粗细加至 `800`（Extra Bold），颜色加深以提高可读性。
  - **交互态反馈**：引入 `.choice-active` 类，当玩家向左/向右拖拽卡牌超过阈值时，对应的选项文字会放大（`scale-1.05`）、透明度全开并变色，提供强力的视觉确认感。
  - **结构优化**：将原有的 `span` 结构改为 `.choice-container` 容器，配合 `border-top` 明确区域划分，增加点击/滑动的暗示感。

- **设计目的**：
  - 解决用户反馈的选项不明显问题，通过动态反馈引导玩家明确决策方向。
  - 保持“弱预告”原则的同时，增强决策动作的确定性。

---

## [2026-02-15] 选项交互升级：点选与滑动并行

- **变更内容**：
  - **按钮化改造**：将卡牌底部的选项文字包装为显式的「选择按钮」。增加微弱背景色（`#f9fafb`）和圆角边框（`12px`），使其在视觉上具备强烈的“可点击感”。
  - **逻辑对齐**：点击按钮时直接调用 `onSwipe(dir)` 逻辑，确保点选与滑动在数值结算、动画翻页、音效反馈上完全一致。
  - **交互鲁棒性**：在 `showingResult`（翻牌展示结果）期间，按钮会自动进入 `disabled` 状态（置灰并禁用点击），防止玩家因多次连点导致逻辑错误。
  - **增强引导感**：在按钮文案前增加「选择：」前缀，进一步降低新玩家的学习门槛。

- **设计目的**：
  - 解决部分玩家不知道卡牌可以滑动的问题，提供显式的操作通道。
  - 提升移动端点击热区，优化整体操作流程的顺滑度。

---

## [2026-02-15] 局内切换身份：顶部入口 + 侧边栏抽屉

- **变更内容**：
  - 在游戏主界面顶部「身份」区域新增 `切换身份 ⇄` 按钮，提供局内可发现的切换入口。
  - 点击后打开右侧抽屉（Drawer），展示全部身份列表，支持在局内快速切换。
  - 切换操作采用 **二次确认**：提示“切换将重置当前进度”，避免误触。
  - 确认后复用 `CONFIRM_IDENTITY` 初始化逻辑，重置当局进度并进入 `intro`，确保会播放开场视频。

- **设计目的**：
  - 降低“想换身份必须退出流程”的阻力，提升探索效率。
  - 通过二次确认与重置策略，保证规则清晰并避免状态混乱。

---

## [2026-02-15] 开发调试功能：DEV_MODE 一键成功结局

- **变更内容**：
  - **调试开关**：引入 `spring_festival_dev_mode` 存储键，通过 localStorage 控制开发面板显隐。
  - **调试面板**：在 App 右上角增加仅开发态可见的“DEV”浮层，支持实时选择身份、指定结局类型（如 perfect_balance）、手动调节数值。
  - **真逻辑注入**：新增 `DEV_FORCE_ENDING` 动作，直接跳过游玩过程进入结局页，且复用正式结算逻辑（写入存档、更新进度、解锁成就、计算解锁提示等）。

- **设计目的**：
  - 解决测试成功结局链条（解锁提示、多身份进度）耗时过长的问题。
  - 确保开发态测试的结果与正式玩家通关的存档变动完全一致。

---

## [2026-02-15] 结局配置清理：移除重复键并恢复缺失结局

---

## [2026-02-15] 身份详情页交互修复：开始按钮即时响应

- **问题描述**：
  - 身份详情展开层点击“进入春节”后无立即反应，需先点“返回”才会触发进入流程，表现为按钮不灵敏。

- **修复内容**：
  - 将“进入春节”按钮改为 `motion.button` 并在点击时 `stopPropagation`，避免事件冒泡被遮罩层吞掉。
  - 点击“进入春节”时先同步 `setActiveId(null)` 关闭展开态，再调用 `onConfirm`，避免动画卸载阻塞造成延迟。

- **设计目的**：
  - 确保点击主 CTA 后立刻进入游戏流程，提升交互确定性。

---

## [2026-02-15] 身份详情页 CTA 强化：进入春节按钮引导

- **变更内容**：
  - 在身份详情展开层将主按钮文案改为 `进入春节（{身份名}）`，并使用更强的主按钮样式（渐变 + 更大字号 + 更强阴影）。
  - 增加展开后的引导文字：明确“已选择身份，点击下方开始进入剧情”。
  - 主按钮增加短时呼吸动效（约 2-3 秒），强化用户下一步操作引导；“返回”按钮降为次要样式。

- **设计目的**：
  - 让用户在展开详情后第一眼锁定主操作，减少犹豫与误操作。

---

## [2026-02-15] 身份选择页交互重构：头像网格与展开详情

- **变更内容**：
  - 身份选择页改为 2 列头像方块网格（头像为本地图片占位，默认路径 `./assets/avatars/<id>.png`，加载失败回退 emoji）。
  - 点击头像即选中该身份，并弹出覆盖层详情卡（共享元素动画 `layoutId`），详情卡包含描述/解锁提示与“开始/返回”。
  - 展开态会覆盖其它头像，避免列表拥挤，支持未来扩展更多身份。
  - 保留既有解锁逻辑（外卖/富二代/分享解锁），并在展开态提供分享解锁入口。

- **设计目的**：
  - 首屏更聚焦“选择”，细节信息按需展开。
  - 为后续增加大量身份预留空间，避免按钮被顶出屏幕或信息密度过高。

---

## [2026-02-15] 身份选择页 UI 修复：列表滚动豁免与按钮可达

- **问题描述**：
  - 身份数量增加后，身份选择页底部“进入春节”按钮被内容顶出屏幕，造成流程阻塞。

- **修复内容**：
  - 身份选择页容器改为 `h-screen + overflow-hidden` 的三段式布局思路。
  - 身份卡片网格区域设置为唯一可滚动区域（`overflow-y-auto + flex-1 + min-h-0`），适配未来拓展大量身份。
  - 底部 CTA（进入春节按钮）设置为 `shrink-0`，确保始终在视口内可见。

- **规则补充**：
  - “禁止滚动/分页展示”原则仅适用于长文本叙事页（卡牌正文/结局/失败等）。
  - 身份选择页属于列表选择界面，允许滚动（列表页豁免）。

- **问题描述**：
  - `ENDINGS_BY_IDENTITY` 对象中曾出现 `waimai` / `fuerdai` 重复键，导致前者被后者覆盖（典型废代码/维护陷阱）。
  - 清理过程中发生误删风险，现已恢复并校正为“每个身份仅一份结局配置”。

- **修复内容**：
  - 确保 `ENDINGS_BY_IDENTITY` 仅包含 4 个身份：`juanwang` / `tizhinei` / `waimai` / `fuerdai`。
  - 恢复并保留 `waimai` / `fuerdai` 的 `failure` 与 `normal` 结局配置，避免运行时读取 `undefined`。

- **设计目的**：
  - 消除重复键覆盖带来的隐性风险，保证结局统计与通关/失败逻辑稳定可维护。

## [2026-02-15] 新增功能：分享解锁「家族继承人」身份（纯前端）

- **变更内容**：
  - **新增存档字段**：在本地存档（`spring_festival_game_v2`）中新增 `sharedUnlocks: string[]`，用于记录“通过分享解锁过的身份”。
  - **解锁逻辑调整**：
    - 当 `fuerdai`（家族继承人）未满足“成功通关三身份”的解锁条件时，会显示为锁定。
    - 新增“分享一次即可解锁”的旁路：若 `sharedUnlocks` 包含 `fuerdai`，则直接视为已解锁。
  - **身份选择页新增入口**：在 `IdentitySelect` 的 `fuerdai` 卡片底部，新增 `分享解锁` 按钮。
    - 点击后自动复制分享文案（含 `?from=share` 链接）到剪贴板，并弹窗提示用户去粘贴分享。
    - 无法复制时会降级为弹窗展示全文，允许手动复制。
    - 完成后写入 `sharedUnlocks` 并立即刷新解锁状态。
  - **分享落地提示**：当 URL 参数包含 `from=share` 时，首次进入会弹出欢迎提示（仅提示一次，避免打扰）。

- **设计目的**：
  - 用最低开发成本增加裂变入口：玩家通过“解锁新身份”获得强动机，分享文案自带拉新链接。
  - 保持纯 H5、无后端、最小改动原则。

---

## [2026-02-15] 故事开场与音频体验优化（短视频 + BGM + 喇叭开关）

- **变更内容**：
  - **开场短视频替换打字机**：`intro` 阶段由 `OpeningVideo` 全屏播放短视频，结束/跳过后进入 `playing`。
  - **按身份视频命名规则**：`./assets/{identityId}_kaitou.mp4`（如 `juanwang_kaitou.mp4` / `fuerdai_kaitou.mp4`）。
  - **带声音播放策略**：默认尝试带声音播放；若浏览器拦截“有声自动播放”，则显示覆盖层提示用户点击后播放（带声）。
  - **减少首帧卡顿（预加载）**：点击“进入春节（{身份名}）”前执行 `video.preload='auto' + load()` 提前拉取对应开场视频资源，降低进入 `intro` 阶段停留在 `loading-bg`（poster）上的时间。

- **全局背景音乐（BGM）**：
  - **资源路径**：`./assets/bgm.mp3`。
  - **播放时机**：仅在 `phase === 'playing'`（选牌主画面）循环播放，离开该阶段自动暂停。
  - **默认音量**：固定为 20%（`volume = 0.2`），避免喧宾夺主。
  - **交互解锁兜底**：监听首次 `pointerdown/touchstart`，在移动端自动播放限制下确保用户第一次交互后可播放。
  - **UI 控制**：在卡牌主画面进度条右侧增加小喇叭按钮，点击可静音/开启（仅影响 BGM）。

- **潜在风险与规避**：
  - **浏览器策略限制**：多数移动端不允许“无交互的有声自动播放”，因此必须依赖用户点击解锁；已做覆盖层提示与交互兜底。
  - **资源缺失容错**：开场视频加载失败会触发 `onError` 并降级跳过，避免流程阻塞。.

---

## [2026-02-15] 调试/测试：从零初始化整局游戏（页面状态 vs 存档）

- **状态分层（必须先分清）**：
  - **运行态页面/游戏状态（Reducer State）**：由 `initialState` + `gameReducer` 管理，包含 `phase/day/round/stats/history/currentCard/...`。
  - **永久存档（localStorage）**：由 `STORAGE_KEY = 'spring_festival_game_v2'` 管理，包含 `unlockedAchievements/completedIdentities/unlockedEndings/successfulIdentities/sharedUnlocks` 等。

- **只想“重新开一局流程”（保留解锁/成就）**：
  - 使用 reducer 动作即可：
    - `RESTART`：回到 `initialState`，并重新 `loadSavedData()`。
    - `BACK_TO_IDENTITY`：回到身份选择页（`phase: 'identity'`），并重新 `loadSavedData()`。

- **想“从零测试整个游戏”（清空所有解锁链条与进度）**：
  - 必须清掉本地存档，否则 `RESTART/BACK_TO_IDENTITY` 会把旧存档再次读回来。
  - Console 一键清档（推荐调试用）：
    - `localStorage.removeItem('spring_festival_game_v2')`
    - （可选）`localStorage.removeItem('spring_festival_share_welcome_shown')`：重置分享落地欢迎弹窗
    - 然后 `location.reload()` 以确保内存态与 UI 完全回到初始

- **建议（后续可做但非必须）**：
  - 在 DEV 面板增加一个“清档并重开”按钮：
    - 删除 `spring_festival_game_v2`
    - 可选删除 `spring_festival_share_welcome_shown`
    - 再 `location.reload()`（比仅 `dispatch('RESTART')` 更“绝对从零”）

---

## [2026-02-15] UI 迭代记录：底部 Tab「人物」与「关于」

- **目标**：
  - 将底部 Tab 的“日志”替换为“人物”，只做纯展示的悬念页，不能引入复杂逻辑。
  - 将“关于”页改为春节祝福信，保持轻量与沉浸。

- **实现要点**：
  - **Tab 替换**：
    - `initialState.activeTab` 注释从 `log` 改为 `people`。
    - `BottomNav` tabs 数组从 `['home','log','achievements','about']` 改为 `['home','people','achievements','about']`。
    - `GameBoard` 非 home 分支中，将 `state.activeTab === 'log'` 替换为 `state.activeTab === 'people'` 并渲染 `PeopleTab`。

  - **人物页（方案 A：档案墙静态海报感）**：
    - 用 `PEOPLE_TEASER` 替代复杂关系链：3 个“匿名档案卡”（代号 + 一句话）+ 2 条“被涂黑关系”。
    - “涂黑关系”使用 `███` 作为遮挡，关系两端使用断裂箭头 `— — —`，避免形成可推理的复杂网。
    - 页面文案去除“解锁/线索库”等机制暗示，仅保留“未整理/预告”的氛围。
    - 点击任何档案/关系仅触发 `alert`：`施工中，档案尚未整理。`（不改状态、不接入存档、不引入逻辑）。

  - **关于页（春节祝福信）**：
    - `GameBoard` 中 `state.activeTab === 'about'` 的渲染块改为“一封春节的信”，用多个 `<p>` 分段呈现，提升可读性。
    - 去掉联系方式字段，避免页面变成广告/导流。

- **设计心得**：
  - 悬念页的关键不是信息量，而是“可感知的缺口”：给玩家一个明确的“还有东西没说完”。
  - 通过匿名代号、涂黑关系与克制交互（统一施工中），让玩家好奇但不困惑，也不会误以为有隐藏系统需要攻略。

---

## [2026-02-15] 修正：富二代分享解锁链接替换

- **变更内容**：
  - **精准定位**：修正了之前在 `assets/data.js` 描述字段误加链接的错误，改为在 `assets/app.js` 的 `onShareUnlock` 逻辑中进行拦截。
  - **逻辑实现**：在生成分享文案 `shareText` 时增加判断：若当前解锁身份为 `fuerdai`（家族继承人），则将文案中的链接强制替换为 `http://i9n.cn/WiIwC`。
  - **覆盖范围**：仅针对富二代角色生效，其他角色的分享链接仍保持动态生成（当前页面 URL），确保不影响正常功能。

- **设计目的**：
  - 满足特定角色的裂变/引流需求，确保“分享解锁”按钮复制到剪贴板的内容符合预期。
  - 保持代码逻辑清晰，避免污染静态配置数据。

---

## [2026-02-15] 移动端小屏适配修复（救命优先：Identity/Playing/Ending/Failure）

- **问题背景**：
  - 小屏手机（尤其 iOS Safari / 带手势条机型）出现“界面看不全、底部按钮/选项被遮挡且无法滚动找回”的问题。
  - 根因通常是 `100vh`（Tailwind `h-screen`）在移动端会把浏览器地址栏/底栏算进去 + 业务侧大量 `overflow-hidden` 禁止滚动，导致底部区域被裁切。

- **总体策略（最小改动原则）**：
  - 引入“动态视口高度”与“安全区”工具类，替换关键页面的 `h-screen`，并把 `pb-safe` 放在最合适的底部区域（底部导航/CTA）。
  - 同时调整 `flex` 收缩关系（`min-h-0` / `shrink-0`），确保关键 CTA 与卡牌左右选项永远露出。

- **全局补丁（index.html）**：
  - 增加可选工具类（不改变未使用页面行为）：
    - `.h-screen-fix`: `height: 100vh; height: 100dvh;`（优先用 `dvh` 贴合可视区）
    - `.pb-safe`: `padding-bottom: env(safe-area-inset-bottom);`（避开手势条/安全区）
  - 增加 `--vh` 兜底：使用 `window.innerHeight` 写入 `--vh` 并监听 `resize`，为不支持 `100dvh` 的环境提供后备。

- **Identity（身份选择页）**：
  - 身份选择页根容器：`h-screen` -> `h-screen-fix`。
  - 身份详情弹窗：改为 `flex` 三段式（标题/描述/CTA），描述区允许滚动，底部 CTA 区加 `pb-safe`，保证“进入春节/返回”按钮在短屏必可见。

- **Playing（GameBoard + SwipeCard）**：
  - GameBoard 根容器：`h-screen` -> `h-screen-fix`，避免底部导航被浏览器 UI 吃掉。
  - BottomNav 本体：根容器增加 `pb-safe`（最稳妥的安全区处理位置）。
  - 卡牌区域：容器改为 `flex-1 min-h-0`，允许在短屏时收缩。
  - 左右选项按钮：`choice-container` 增加 `shrink-0`，确保两侧选项在小屏下绝对露出、不被卡牌正文挤没。

- **Ending/Failure（结算页）**：
  - 根容器：`h-screen` -> `h-screen-fix`，并加 `pb-safe`。
  - 底部 CTA 双保险：CTA 区额外包裹 `shrink-0 pb-safe`，防止未来改动导致按钮再次贴底/被遮挡。

- **验收清单（必须验证）**：
  - iPhone SE / 320x568：
    - Identity 详情弹窗底部 CTA 可见可点。
    - Playing 卡牌底部左右选项“选择：xxx”完整露出。
    - BottomNav 不被手势条遮挡。
    - Ending/Failure 底部 CTA 可见可点。
  - iOS Safari / 微信内置浏览器：切换地址栏显示/隐藏时布局不跳出可用区域。

---

## [2026-02-15] 移动端小屏适配修复（救命优先：Identity/Playing/Ending/Failure）

- **问题背景**：
  - 小屏手机（尤其 iOS Safari / 带手势条机型）出现“界面看不全、底部按钮/选项被遮挡且无法滚动找回”的问题。
  - 根因通常是 `100vh`（Tailwind `h-screen`）在移动端会把浏览器地址栏/底栏算进去 + 业务侧大量 `overflow-hidden` 禁止滚动，导致底部区域被裁切。

- **总体策略（最小改动原则）**：
  - 引入“动态视口高度”与“安全区”工具类，替换关键页面的 `h-screen`，并把 `pb-safe` 放在最合适的底部区域（底部导航/CTA）。
  - 同时调整 `flex` 收缩关系（`min-h-0` / `shrink-0`），确保关键 CTA 与卡牌左右选项永远露出。

- **全局补丁（index.html）**：
  - 增加可选工具类（不改变未使用页面行为）：
    - `.h-screen-fix`: `height: 100vh; height: 100dvh;`（优先用 `dvh` 贴合可视区）
    - `.pb-safe`: `padding-bottom: env(safe-area-inset-bottom);`（避开手势条/安全区）
  - 增加 `--vh` 兜底：使用 `window.innerHeight` 写入 `--vh` 并监听 `resize`，为不支持 `100dvh` 的环境提供后备。

- **Identity（身份选择页）**：
  - 身份选择页根容器：`h-screen` -> `h-screen-fix`。
  - 身份详情弹窗：改为 `flex` 三段式（标题/描述/CTA），描述区允许滚动，底部 CTA 区加 `pb-safe`，保证“进入春节/返回”按钮在短屏必可见。

- **Playing（GameBoard + SwipeCard）**：
  - GameBoard 根容器：`h-screen` -> `h-screen-fix`，避免底部导航被浏览器 UI 吃掉。
  - BottomNav 本体：根容器增加 `pb-safe`（最稳妥的安全区处理位置）。
  - 卡牌区域：容器改为 `flex-1 min-h-0`，允许在短屏时收缩。
  - 左右选项按钮：`choice-container` 增加 `shrink-0`，确保两侧选项在小屏下绝对露出、不被卡牌正文挤没。

- **Ending/Failure（结算页）**：
  - 根容器：`h-screen` -> `h-screen-fix`，并加 `pb-safe`。
  - 底部 CTA 双保险：CTA 区额外包裹 `shrink-0 pb-safe`，防止未来改动导致按钮再次贴底/被遮挡。

- **验收清单（必须验证）**：
  - iPhone SE / 320x568：
    - Identity 详情弹窗底部 CTA 可见可点。
    - Playing 卡牌底部左右选项“选择：xxx”完整露出。
    - BottomNav 不被手势条遮挡。
    - Ending/Failure 底部 CTA 可见可点。
  - iOS Safari / 微信内置浏览器：切换地址栏显示/隐藏时布局不跳出可用区域。
